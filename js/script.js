$(document).ready(function(){
    $('.carousel').carousel({
        interval: 3000, // Intervalo de tiempo en milisegundos entre slides
        pause: 'hover' // Pausa el carrusel en hover
    });
});